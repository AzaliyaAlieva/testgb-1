a.

В задании где нужно удалить строки и поменять маленькие буквы на заглавные я разобрал ваше решение, 
но свое сделал на  sed,но часть заимствовал.

echo "Enter the filename:"
read filename

if [ -f $filename ]; then
  sed -i 's/.*/\U&/' $filename
  sed '/./!d' $filename
fi


b.
Тут разве что смог запустить, маны почитал, но такое я могу только как готовое решение взять. 
Вот такой вывод при попытке логина из pitty по ssh.
trosbalos@trosbalos-VirtualBox:~/hw3$ bash 1.sh 
\n
Attention! Authentication failure!
\n
Attention! Authentication failure!
 
Не очень понял в чем смысл:
echo "\n"


c.

Сам я не в состоянии написать такой вот скрипт, но в состоянии проанализировать и когда я запустил Ваше решение оказалось,
 что скрип не работает корректно. А именно не создает папки.
Подходящего ключа к mv не нашел и поэтому решил добавить создание каталогов по именам переменной в цикле до перемещения файлов.
В таком виде работате кореектно.
for i in {2013..2019}
	do
		for j in {01..12}
			do
				for d in {01..31}
					do
						for h in {01..24}
						  do
						    echo $i$j$d$h.txt > $i$j$d$h.txt
						  done
						mkdir -p $i/$j/$d
					  mv $i$j$d* $i/$j/$d
					done
			done
	done


